name: Atualizar dados do observatório

on:
  schedule:
    # Diário às 06:00 UTC (03:00 BRT)
    - cron: "0 6 * * *"
  workflow_dispatch: # permite rodar manualmente

permissions:
  contents: write
  issues: write

jobs:
  update:
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - uses: actions/checkout@v5

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install anthropic

      - name: "[1/13] Rebuild autoria (fetch Câmara API)"
        run: python -u scripts/rebuild_autoria.py
        timeout-minutes: 40

      - name: "[2/13] Enrich autoria (sexo, gender_stats)"
        run: python -u scripts/enrich_autoria.py

      - name: "[3/13] Classify stance (regex)"
        run: python -u scripts/classify_stance.py

      - name: "[4/13] Penalidade por voto regressivo (seed editorial)"
        run: python -u scripts/compute_vote_penalty.py
        timeout-minutes: 10
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: "[5/13] Rebuild coerência (votos nas 4 PLs de mérito)"
        run: python -u scripts/rebuild_coerencia.py
        timeout-minutes: 10

      - name: "[6/13] Rebuild relatoria (designações por sexo)"
        run: python -u scripts/rebuild_relatoria.py
        timeout-minutes: 30

      - name: "[7/13] Rebuild articuladores (top 3 por UF)"
        run: python -u scripts/rebuild_articuladores.py

      - name: "[8/13] Rebuild legislativo (tramitação)"
        run: python -u scripts/rebuild_legislativo.py
        timeout-minutes: 20

      - name: "[9/13] Enrich votacoes (autor + relator)"
        run: python -u scripts/enrich_votacoes.py

      - name: "[10/13] Detectar novas votações + interpretação LLM"
        run: python -u scripts/update_votacoes.py
        timeout-minutes: 20
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}

      - name: "[11/13] Rebuild destaque (1 grande fato recente)"
        run: python -u scripts/rebuild_destaque.py
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}

      - name: "[12/13] Rebuild Senado Federal"
        run: python -u scripts/rebuild_senado.py
        timeout-minutes: 15

      - name: "[13/13] Fetch candidatos TSE 2026 (silencia se indisponível)"
        run: python -u scripts/fetch_candidatos_tse.py

      - name: "[guard] Invariantes do pipeline (regressivos/punitivistas)"
        run: python -u scripts/check_pipeline_invariants.py

      - name: Check for changes
        id: changes
        run: |
          git diff --quiet src/data/ && echo "changed=false" >> $GITHUB_OUTPUT || echo "changed=true" >> $GITHUB_OUTPUT

      - name: Commit and push
        if: steps.changes.outputs.changed == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add src/data/ scripts/destaque_llm_cache.json
          git commit -m "chore: atualização automática de dados $(date -u +%Y-%m-%d)"
          git pull --rebase --autostash origin main
          git push

  # Classificação LLM — roda só às segundas (custo ~$0.85/run)
  classify-llm:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    needs: update
    if: github.event.schedule == '0 6 * * 1' || github.event_name == 'workflow_dispatch'

    steps:
      - uses: actions/checkout@v4
        with:
          ref: main  # pega commits do job anterior

      - name: Pull latest (caso update tenha commitado)
        run: git pull origin main

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install anthropic

      - name: "Classify stance via LLM (Haiku 4.5)"
        run: python -u scripts/classify_stance_llm.py
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}

      - name: "Re-enrich after LLM"
        run: |
          python -u scripts/enrich_autoria.py
          python -u scripts/compute_vote_penalty.py
          python -u scripts/rebuild_articuladores.py
          python -u scripts/rebuild_legislativo.py

      - name: "[guard] Invariantes do pipeline (regressivos/punitivistas)"
        run: python -u scripts/check_pipeline_invariants.py

      - name: Check for changes
        id: changes
        run: |
          git diff --quiet src/data/ && echo "changed=false" >> $GITHUB_OUTPUT || echo "changed=true" >> $GITHUB_OUTPUT

      - name: Commit and push
        if: steps.changes.outputs.changed == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add src/data/
          git commit -m "chore: reclassificação LLM semanal $(date -u +%Y-%m-%d)"
          git pull --rebase --autostash origin main
          git push
