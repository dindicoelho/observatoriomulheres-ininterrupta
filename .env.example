# Captação de email (opcional — só necessário se quiser testar
# o formulário de inscrição localmente). Cria um projeto grátis em
# https://supabase.com e copia URL + anon key do dashboard.
NEXT_PUBLIC_SUPABASE_URL=https://seu-projeto.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOi...

# Classificação LLM (só usada pelos scripts Python do pipeline,
# nunca pelo frontend). Necessária pra rodar scripts/classify_stance_llm.py
# e scripts/update_votacoes.py localmente.
ANTHROPIC_API_KEY=sk-ant-...
